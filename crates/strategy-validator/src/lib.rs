//! TODO(Phase 4): validates strategy-definition JSON against the frozen v1.0 format.
//! Single validator all three front doors (visual builder, JSON API, MCP) target.
pub mod expressions;
pub mod risk;
pub mod schema;
