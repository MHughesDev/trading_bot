# Local development overrides — values here take precedence over default.toml.
# Secrets (API keys) come from environment variables, not this file.

[platform]
port = 8080

[api]
host = "127.0.0.1"
port = 8080

[data]
default_watermark_secs = 2
