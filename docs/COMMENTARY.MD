# Commentary

Architecture and gap narrative: **[`QUEUE.MD`](QUEUE.MD)** — open roadmap, resolved fixes (`IL-*`), gates (`HG-*`), and completed archive (`FB-*`).

For risk ordering and shutdown: [`RISK_PRECEDENCE.MD`](RISK_PRECEDENCE.MD), [`GRACEFUL_SHUTDOWN.MD`](GRACEFUL_SHUTDOWN.MD).
