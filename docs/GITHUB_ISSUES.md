# GitHub issues (optional)

**Canonical backlog log (GitHub-style, markdown):** [`ISSUE_LOG.md`](ISSUE_LOG.md)

Use that file to track status locally. To create real GitHub issues, copy each `# Issue N` section into **New issue**, or run `scripts/create_github_issues.sh` if you use `gh` CLI.
