# trading-system-spec-package (canonical rebuild)

This archive is the cleaned canonical rebuild.

## Structure

- `canonical/` — authoritative documents
- `superseded/` — older or overlapping drafts retained only for historical reference

## Canonical source of truth

Start with:
- `canonical/APEX_UNIFIED_Full_System_Master_Spec_v2_1_CANONICAL.md`

Then use the supporting canonical specs in the same folder.

## Why this rebuild was needed

The prior package had two problems:
1. the file named as the full-system master spec had been overwritten by a duplicate of the feature/data-contract spec
2. older overlapping drafts were mixed into the package without clear labeling

This rebuilt package fixes both issues.
