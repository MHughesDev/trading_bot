# Canonical migration — deletion log

Records **removed** paths and **replacement** pointers so contributors can trace what left the tree and why. Complements [`reports/REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md`](reports/REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md) §5.1.

**Commit links:** see **[`CANONICAL_TOMBSTONE_INDEX.MD`](CANONICAL_TOMBSTONE_INDEX.MD)** (FB-CAN-058) for a table with GitHub permalinks per removed path.

| Removal (commit / slice) | What was removed | Replacement / notes |
|--------------------------|------------------|---------------------|
| FB-CAN-012 (2026-04-18) | `docs/Specs/MASTER_SPEC_RISK_STATE_GAP.MD` | Superseded by the repo vs canonical gap audit ([`REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md`](reports/REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md)) and APEX canonical risk/state specs. For app vs policy `RiskState` naming, see [`Specs/RISK_ENGINE.MD`](Specs/RISK_ENGINE.MD) and [`app/contracts/risk.py`](../app/contracts/risk.py). Historical audit reference: **FB-AUDIT-12** in [`QUEUE_ARCHIVE.MD`](QUEUE_ARCHIVE.MD). |
| FB-CAN-019 (2026-04-18) | `decision_engine/action_generator.py`, `tests/test_action_generator_routes.py` | Standalone route→`forecast.returns_5`→proposal helper was unused on the live/replay path (`run_decision_tick` → `DecisionPipeline` → `PolicySystem` + `run_spec_policy_step`). Proposal sizing semantics remain in [`spec_policy_proposal.py`](../decision_engine/spec_policy_proposal.py) (`SPEC_POLICY_DELTA_TO_SIZE_FRACTION_SCALE`). |
| FB-CAN-020 (2026-04-18) | `risk_engine/equal_weight.py`, `tests/test_equal_weight.py` | Deferred **FB-N3** reference stub not wired into `RiskEngine`; canonical sizing is [`canonical_sizing.py`](../risk_engine/canonical_sizing.py). Historical queue ref: **FB-N3-S1** in [`QUEUE_ARCHIVE.MD`](QUEUE_ARCHIVE.MD). |
| FB-CAN-021 (2026-04-18) | `get_execution_adapter` in [`execution/router.py`](../execution/router.py) | Thin duplicate of **`create_execution_adapter`**; all call sites use **`create_execution_adapter`** (`ExecutionService`, tests). |
| FB-CAN-022 (2026-04-18) | Top-level **`risk:`** block in `default.yaml` | Superseded by **`apex_canonical.domains.risk_sizing`** + flat `AppSettings` / **`NM_RISK_*`**; **`load_settings`** rejects YAML **`risk:`**. |
| FB-CAN-023 (2026-04-18) | *(no file deletions — archive-by-clarification)* | Added **[`docs/Specs/CANONICAL_PRECEDENCE.MD`](Specs/CANONICAL_PRECEDENCE.MD)** and **tombstone** notes on key **`docs/Specs/*.MD`** files so contributors do not treat as-built docs as **APEX** requirements; updated **[`Human Provided Specs/README.MD`](../Human%20Provided%20Specs/README.MD)** and **gap audit §5.1** ([`REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md`](../reports/REPO_VS_CANONICAL_SPECS_GAP_AUDIT.md)). |
| FB-CAN-024 (2026-04-18) | `tests/test_spec_pipeline_mode.py` | Overlapped **`test_pipeline_forecast_packet.py`**; removed and folded **`test_default_settings_have_forecaster_fields`** + canonical assertions into that file; **`test_decision_risk.py`** now checks merged **`RiskState`** after **`pipeline.step`**. See [`Specs/TESTING_AND_CI.MD`](Specs/TESTING_AND_CI.MD). |
| FB-CAN-025 (2026-04-18) | *(no runtime deletions)* | Added **`scripts/ci_canonical_gates.sh`** (aggregate: `ci_canonical_config_gates.py`, `ci_forbidden_legacy_paths.sh`, `ci_replay_determinism.py`); extended **`ci_queue_consistency.py`** for Open **`CAN-*`** batches vs §2.7 canonical table; **`tests/test_ci_queue_consistency.py`**; CI job in **`.github/workflows/ci.yml`**. **`DecisionPipeline.step`** passes **`data_timestamp`** into **`build_forecast_packet_methodology(..., now=...)`** so cyclical features are replay-deterministic when the bar timestamp is set. |
| FB-CAN-026 (2026-04-18) | *(no deletions)* | **`orchestration/release_evidence.py`** (`ReleaseEvidenceBundle`, canonical fingerprint, structured diff); **`scripts/build_release_evidence_bundle.py`**; **`GET /governance/release-evidence`**, **`POST /governance/release-evidence/diff`**; see [`GOVERNANCE_RELEASE_AND_EXPERIMENTS.MD`](GOVERNANCE_RELEASE_AND_EXPERIMENTS.MD). |
| FB-CAN-027 (2026-04-18) | *(no deletions)* | Experiment registry: lifecycle + **`/governance/experiments`**; bidirectional **`linked_release_candidate`** ↔ **`release_ledger.json`** `linked_experiment_ids`; **`research/__init__.py`**. |
| FB-CAN-028 (2026-04-18) | *(no deletions)* | Canonical alerting: **`infra/prometheus/alerts/canonical_apex.yml`**, Grafana dashboard **`tb-canonical-health`**, **`GET /governance/monitoring`**, tests; see [`MONITORING_CANONICAL.MD`](MONITORING_CANONICAL.MD). |
| FB-CAN-029 (2026-04-18) | *(no deletions)* | **`decision_engine/canonical_orchestrator.py`** — explicit canonical sequence after forecast; **`DecisionPipeline.step`** delegates; docs in **`run_step.py`** and **`live_service.py`**. |
| FB-CAN-030 (2026-04-18) | *(no deletions)* | Live–replay equivalence: **`backtesting/live_replay_equivalence.py`**, **`ci_live_replay_equivalence.py`**, promotion **`EvidencePackage`** fields + live gates for **logic**/**combined**. |
| FB-CAN-031 (2026-04-18) | *(no deletions)* | Missed-move + false-positive memory: **`trigger_engine`**, **`RiskState.trigger_false_positive_memory`**, **`auction_engine`** P penalty, replay safety event. |
| FB-CAN-032 (2026-04-18) | *(no deletions)* | Canonical **signal_confidence** domain + **feature_families** flags; **`apply_signal_family_confidence`** in boundary input merge; CI validates `default.yaml`. |
| FB-CAN-033 (2026-04-18) | *(no deletions)* | **`classify_hard_override`** + **`RiskState`** transition/occupancy; **`canonical_orchestrator`** short-circuit **NO_TRADE**; **`pipeline.step`** passes **`feed_last_message_at`** / **`now_ref`**; replay **`safety_snapshot_event`**; metrics **`tb_canonical_hard_override_total`**, **`tb_canonical_degradation_transition_count`**. |
| FB-CAN-034 (2026-04-18) | *(no deletions)* | Auction diversification: **`D_corr`**, **`D_thesis`**, **`D_liq`** → combined **`D`**; **`AuctionResult.clustering_metadata`** (thesis buckets, liquidation cluster key); replay via **`forecast_diagnostics["auction"]`**. |
| FB-CAN-035 (2026-04-18) | *(no deletions)* | **`execution_feedback_memory`**: EMA + **`DecisionPipeline`** / **`run_decision_tick`**; replay **`replay.py`** + live **`live_service`**; trigger/auction scale **`exec_conf`** by **`canonical_exec_quality_penalty`**. |
| FB-CAN-036 (2026-04-18) | *(no deletions)* | **`app/contracts/decision_record.py`**; **`build_decision_record`**; **`RiskEngine.last_risk_block_codes`**; **`RiskState.last_decision_record`**; **`GET /governance/decision-record`**; replay **`decision_output_event`**. |
| FB-CAN-037 (2026-04-18) | *(no deletions)* | **`orchestration/fault_injection_profiles.py`** (named profiles); **`ReplayRunContract.fault_injection_profile_id`**; extended **`backtesting/fault_injection.py`**; **`ReleaseEvidenceBundle`** / **`EvidencePackage`** fault-stress fields; shadow/live **`fault_stress_evidence`** gate; **`scripts/run_fault_profile_replay.py`**. |
| FB-CAN-038 (2026-04-18) | *(no deletions)* | **`app/config/shadow_comparison.py`** + **`apex_canonical.domains.shadow_comparison`**; **`orchestration/shadow_comparison.py`**; **`EvidencePackage.shadow_comparison_*`** + promotion gates; **`ReleaseEvidenceBundle.shadow_comparison_report`**; **`models/registry/shadow_comparison_store.py`**; **`GET/POST /governance/shadow-comparison`**; **`scripts/compare_shadow_replays.py`**. |
| FB-CAN-060 (2026-04-18) | Migration-only domain **`projection`** keys (value **legacy**) in canonical **`AppSettings`** synthesis | Renamed **`synthesize_canonical_from_legacy`** → **`synthesize_canonical_from_app_settings`**; domain bags use **`source: app_settings`**; CI guard **`scripts/ci_no_migration_projection_legacy.py`** (see **`scripts/ci_canonical_gates.sh`**). |
| FB-CAN-061 (2026-04-18) | *(no file deletions)* | **`CanonicalMetadata`** validation + **`validate_canonical_runtime_metadata`** after **`resolve_canonical_config`**; production gate when **`NM_EXECUTION_MODE=live`** or **`NM_CANONICAL_CONFIG_STRICT=1`**; **`default.yaml`** **`created_at`** + **`environment_scope: live`** aligned with default **`execution.mode: live`**; CI **`ci_canonical_config_gates`** extended for §4 fields. |
| FB-CAN-062 (2026-04-18) | *(no file deletions)* | **`app/contracts/canonical_conventions.py`** — UTC timestamps, **[0,1]** confidence/freshness clipping, **`validate_decision_boundary_input_timestamps`**; **`decision_snapshots`** validators; **`snapshot_builders`** post-build check. |
| FB-CAN-063 (2026-04-18) | *(no file deletions)* | **`app/contracts/reason_codes.py`** — stable prefixed codes (**`trg_*`**, **`auc_*`**, **`exe_*`**, **`pip_*`**, **`ovr_*`**, **`state_*`**) + **`normalize_reason_code(s)`** for legacy strings; trigger/auction/execution/pipeline/state emit canonical codes; **`build_decision_record`** normalizes **`no_trade`** / **`safety_override`** lists and merges **`apex.novelty_reason_codes`** into **`no_trade_reason_codes`**. |
| FB-CAN-064 (2026-04-18) | *(no file deletions)* | Carry monitoring: **`CarrySleeveDecision`** **`trigger_confidence`** + **`decision_quality`**; Prometheus **`tb_canonical_carry_*`** (trigger confidence, decision quality, reason counter, directional suppression); Grafana **tb-canonical-health** carry panels; alerts **`CanonicalCarryDirectionalSuppressionSpike`**, **`CanonicalCarryDecisionQualityLow`**; **`GET /governance/monitoring`** lists carry metric families; monitoring domain checklist extended. |
| FB-CAN-065 (2026-04-18) | *(no file deletions)* | **`observability/governance_metrics.py`** — **`tb_governance_*`** counters (promotion attempts, gate outcomes/failures, config drift events, rollback events); **`evaluate_promotion_gates`** + **`POST /governance/release-evidence/diff`** + **`POST /governance/release-objects`** (rolled_back); Grafana + alert group **`canonical_governance_operator_fb_can_065`**; governance domain checklist extended. |
| FB-CAN-066 (2026-04-18) | *(no file deletions)* | **`EvidencePackage`** extended (**`schema_version`**, **`scenario_test_run_ids`**, **`key_metrics`**, **`failure_modes_documented`**); **`orchestration/evidence_package_validation.py`** + gate **`evidence_*`** ids; CI fixture **`tests/fixtures/canonical_release_candidate_live.json`** updated. |
| FB-CAN-067 (2026-04-18) | *(no file deletions)* | **`scripts/ci_canonical_magic_constants.py`** + allowlist **`scripts/ci_canonical_magic_constants_allowlist.txt`**; wired into **`ci_canonical_gates.sh`**; **`docs/Specs/TESTING_AND_CI.MD`**. |
| FB-CAN-068 (2026-04-18) | *(no file deletions)* | **`backtesting/replay_provenance.py`** — contract identity hash, dataset-derived fallback seed, **`resolve_replay_seed`**; **`replay_decisions`** / **`replay_multi_asset_decisions`** attach **`replay_provenance`** to every row; tests **`tests/test_replay_provenance_fb_can_068.py`**; see [`architecture/backtesting_simulator.md`](architecture/backtesting_simulator.md). |

**As-built Specs retained:** `docs/Specs/*.MD` stays the **implementation** map (see [`CANONICAL_MODULE_MAP.MD`](CANONICAL_MODULE_MAP.MD)); gap-audit §5.1 wholesale removal of those files is **not** required for canonical alignment. Per **FB-CAN-023**, use **[`CANONICAL_PRECEDENCE.MD`](Specs/CANONICAL_PRECEDENCE.MD)** when a doc’s authority is unclear; use later migration queue items if a specific file is truly obsolete.
